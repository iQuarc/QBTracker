<UserControl x:Class="QBTracker.Views.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:QBTracker.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="694" d:DesignWidth="410">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0 0 0 1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button
                    Style="{DynamicResource MaterialDesignFloatingActionMiniButton}"
                    Command="{Binding GoBack}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    IsCancel="True"
                    Margin="10">
                    <materialDesign:PackIcon Kind="StepBackward" />
                </Button>
                <TextBlock Grid.ColumnSpan="3"
                           Margin="60,0"
                           Foreground="{StaticResource MaterialDesignBody}"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center">
                    Generate Invoice
                </TextBlock>
                <Button Grid.Column="1"
                        Margin="10"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                        ToolTip="Invoice configuration"
                        Command="{Binding OpenConfiguration}">
                    <materialDesign:PackIcon Kind="CogOutline" Width="22" Height="22" />
                </Button>
                <Button Grid.Column="2"
                        Margin="10"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignFloatingActionMiniSecondaryButton}"
                        ToolTip="Generate invoice"
                        Command="{Binding GenerateInvoice}">
                    <materialDesign:PackIcon Kind="ReceiptTextSendOutline" Width="22" Height="22" />
                </Button>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="10,10,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <DatePicker Grid.Column="0"
                        Margin="0,0,5,0"
                        SelectedDate="{Binding StartDate}"
                        materialDesign:HintAssist.Hint="Start date"
                        Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
            <DatePicker Grid.Column="1"
                        Margin="5,0,0,0"
                        SelectedDate="{Binding EndDate}"
                        materialDesign:HintAssist.Hint="End date"
                        Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
        </Grid>
        <Grid Grid.Row="2" Margin="10,10,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <DatePicker Grid.Column="0"
                        Margin="0,0,5,0"
                        SelectedDate="{Binding InvoiceDate}"
                        materialDesign:HintAssist.Hint="Invoice date"
                        Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
            <TextBox Grid.Column="1"
                     Margin="5,0,0,0"
                     Text="{Binding InvoiceNumber, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Invoice number"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
        </Grid>
        <materialDesign:Card Grid.Row="3" Margin="5">
            <StackPanel>
                <Border Margin="12,12,12,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding InvoiceSettings.InvoiceClientShortName}" FontWeight="Bold" />
                            <TextBlock Text="{Binding InvoiceSettings.InvoiceClientLegalName}" />
                            <TextBlock Text="{Binding InvoiceSettings.InvoiceCurrency, StringFormat=Currency: {0}}" />
                        </StackPanel>
                    </Grid>
                </Border>
                <Expander Header="{Binding ProjectsTitle, Mode=OneWay}" IsExpanded="True">
                    <ItemsControl ItemsSource="{Binding InvoiceProjects}" Grid.IsSharedSizeScope="True" Margin="12,0,12,12">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewModels:InvoiceProject}">
                                <Border x:Name="Border" Padding="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition SharedSizeGroup="InvoiceCheck" Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="90" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox VerticalAlignment="Center" IsChecked="{Binding IsSelected}" />
                                        <StackPanel Grid.Column="1" Margin="8,0,8,0">
                                            <TextBlock Text="{Binding ProjectName}" FontWeight="Bold" />
                                            <TextBlock Text="{Binding Description}" Style="{StaticResource MaterialDesignCaptionTextBlock}" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="2"
                                                   Margin="8,0"
                                                   VerticalAlignment="Center"
                                                   Text="{Binding HoursDisplay}" />
                                        <TextBox Grid.Column="3"
                                                 Margin="8,0"
                                                 VerticalContentAlignment="Center"
                                                 Text="{Binding HourlyRate, UpdateSourceTrigger=LostFocus}"
                                                 materialDesign:HintAssist.Hint="Rate"
                                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                                        <TextBlock Grid.Column="4"
                                                   Margin="8,0,0,0"
                                                   VerticalAlignment="Center"
                                                   Text="{Binding AmountDisplay}" />
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource MaterialDesignSelection}" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
